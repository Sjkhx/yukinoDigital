# 项目约定

## 无视觉能力(重要)

当前接入的模型(deepseek-v4 系列)没有视觉能力,发送图片会导致上游 API 返回 400 并中断对话:

- **禁止**用 Read 工具读取图片文件(png/jpg/jpeg/gif/webp/bmp/ico 等)。
- **禁止**调用会返回图片内容的工具(浏览器截图、MCP 图片输出等)。
- 需要了解图片内容时,改用文本方式描述:文件路径、`ls` 输出、文件大小、或用脚本读取图片的元数据/文本文件。
- 图片一旦进入会话历史,每轮请求都会重发,整个对话会被反复 400 卡死,只能 `/clear` 重来。
